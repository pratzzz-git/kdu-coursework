type Movie {
  id: ID!
  title: String!
  genre: String!
  director: Director
  reviews: [Review]
}

type Review {
  comment: String!
  rating: Int!
}

type Mutation {
  addReview(movieId: ID!, comment: String!, rating: Int!): Movie
}
